# Проект Skymarket

## Автор:

### Вячеслав Васильев

## Цель проекта:

**Разработка функционала - ядра для интернет-магазина, с учебной целью.
В дальнейшем для этого ядра возможно будет реализовать любой интерфейс (сайт, бот).**

## Инструкция по установке:

**1. Клонируйте репозиторий:**

[GitHub - Skymarket] (https://github.com/VasilyevVV/Skymarket)

## Описание модулей и функций

### Модуль base_product
Объявлен базовый абстрактный класс **BaseProduct**, родительский класс для классов продуктов.
Содержит абстрактный класс-метод **new_product** - создание нового продукта (реализация описана в классе **Product**).

### Модуль print_mixin
Содержит класс-миксин **PrintMixin**, который добавлен в цепочку наследования класса **Product**.\
В классе реализован метод **__repr\__** - при создании объекта выводит в консоль информацию
о классе и параметрах созданного объекта в формате:\
"<Название класса>('<Имя продукта>, <Описание продукта>, <Цена>, <Количество>)" \
где название класса созданного объекта, может быть, например, **Product** (или его наследники).

### Модуль Product

Содержит методы и свойства класса **Product** и его классов-наследников - \
**Smartphone** («Смартфон») и **LawnGrass** («Трава газонная»)\ 

**Атрибуты класса Product:**

- **name** - название,
- **description** - описание,
- **__price** - цена (приватный атрибут),
- **quantity** - количество в наличии.
Для класса **Smartphone** определены дополнительные атрибуты:\
- **efficiency** - производительность,
- **model** - модель,
- **memory** - объем встроенной памяти,
- **color** - цвет
Для класса **LawnGrass** определены дополнительные атрибуты:\
- **country** - страна-производитель,
- **germination_period** - срок прорастания,
- **color** - цвет

#### Методы класса Product
**__init\__** - создает товар - экземпляр класса **Product**.\
При создании товара с нулевым количеством, вызывается исключение ValueError\
с сообщением: "Товар с нулевым количеством не может быть добавлен" и 
выполнение программы прерывается.

#### Магические методы:
- **__str\__** - для строкового отображения класса **Product** в виде:\
<Название продукта>, <Цена> руб. Остаток: <Х> шт.
- **__add\__** - возвращает сумму произведений цены на количество у двух продуктов.
#### Методы класса:
- **price** - метод-геттер для доступа к цене продукта 

- **price** - метод-сеттер для изменения цены продукта.\
Проверяет значение новой цены: если цена отрицательная,\
выводится соответствующее сообщение в консоль и цена не изменяется.\
Нулевую цену установить можно.\
Если цена товара понижается, пользователю необходимо подтвердить изменение вручную:
  - **"Y"** ("yes") - для согласия понизить цену
  - **"N"** ("no") - для отмены действия (цена остаётся неизменной).

- **new_product** - класс-метод для создания экземпляра класса **Product**.\
Принимает на вход параметры товара в виде словаря и возвращает созданный объект класса 
Product.\
 Метод проверяет наличие товара (объекта Product) с таким же именем:
  - если такой уже существует, складывается количество имеющегося в наличии товара и новое количество.
  - при конфликте цен выбирается та, которая является более высокой.
    
Для классов **Smartphone** и **LawnGrass** переопределены методы:
* **__init\__** - с использованием функции **super()** для доступа к конструктору\
родительского класса **Product** и добавления специфичных для этих классов свойств. 
* **__add\__** - в каждом из субклассов метод дополнен проверкой на соответствие типа объекта\
нужному классу таким образом, чтобы можно было складывать товары только из одинаковых классов.\
Т.е. функция **__add\__** не позволяет сложить, например, смартфон и траву газонную.
В случае ошибка возникает исключение TypeError. 

#### Для хранения всех создаваемых объектов класса Product используется приватное свойство **__product_list**
Алгоритм работы с классом **Product** предусматривает создание экземпляра класса **без привязки к какой-либо категории.**\
Класс-метод создания нового экземпляра класса создан с учётом такого принципа создания объектов класса **Product**.


### Модуль Category

Содержит методы и свойства класса **Category**\
**Атрибуты класса:**

- **name** - название,
- **description** - описание,
- **__products** - приватный, содержит список товаров в категории - экземпляров класса Product.

Доступные для каждого объекта класса атрибуты\
с общей информацией для всех объектов:

- **category_count** - количество категорий,
- **product_count** - количество товаров (продуктов для каждой категории).
#### Методы класса Category
#### Магический метод **__str\__** 
- для строкового отображения класса **Category** в виде:\
<Название категории>, количество продуктов: <Х> шт.\
Количество продуктов считается из общего числа всех продуктов в списке товаров категории.
Общее количество складывается из атрибута "Количество" (quantity) каждого товара.
#### Методы класса
 - **products** - метод-геттер возвращает список товаров в виде строк в формате:\
"<Название продукта>, 80 руб. Остаток: 15 шт."
 - **add_product** - добавляет продукт в список товаров в категории - в атрибут **products**
 - **product_in_category** - метод-геттер для доступа к списку продуктов в категории (атрибуту **__products**)
 - **middle_price** - етод рассчитывает среднюю цену для товаров в категории.
Если в категории товары отсутствуют, возвращает 0.


### Модуль **utils**

Загружает данные по категориям и товарам из файла JSON.\
Содержит функции:\
**read_json** - читает данные из JSON-файла и преобразует их в словарь (dict).\
Если файл не найден, возвращает пустой словарь.

**create_objects_from_json** - на основе чтения данных из словаря на входе функции создаёт объекты -
экземпляры классов **Category** и **Product** - товары (**Product**) рапределяются по категориям.


Файл JSON **"products.json"**, из которого загружаются данные для созданий категорий и товаров, находится в каталоге **data**.

## Тестирование (Pytest)

### Тестирование классов и их методов выполняется в файле "test_classes.py"

Тестируются:
 - Конструкторы классов **Product** и **Category**
 - Конструкторы классов **Smartphone** и **LawnGrass**
 - Корректность подсчёта количества категорий и продуктов для класса **Category**
 - Добавление продукта в список товаров в отдельной категории
 - Выод списка товаров в категории
 - Создание экземпляра класса **Product** и изменение количества и цены при создании товара с таким же названием\
 - Строковые представления для классов **Product** и **Category**
 - Магический метод **__add\__** для сложения 2-х товаров (экземпляров класса **Product**)
 - Методы **__add\__** для классов **Smartphone** и **LawnGrass** на случай сложения объектов одного класса
 - Методы **__add\__** для случая сложения объектов разных классов и вызова исключения
 - Функция чтения json-файла в модуле **utils**
 - Функция создания экземпляров классов **Category** и **Product** в модуле **utils**
 - Для тестирования создан файл **test_products.json** в каталоге **tests**
 - Метод **__repr\__** класса **PrintMixin** - печать в консоль информации о классе и параметрах созданного объекта.
 - Создание товаров - экземпляров классов **Product**, **Smartphone** и **LawnGrass** с нулевым количеством и возникновение при этом исключений
 - Расчёт средней цены товаров в категории, с учётом обработки исключения, если в категории нет товаров

 
#### В файле "conftest.py" находятся фикстуры с экземплярами классов, субклассов и словарями для тестирования.
